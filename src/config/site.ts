import { SiteConfig } from '@/types';

export const siteConfig: SiteConfig = {
  name: "Poll",
  description: "Create instant, real-time polls for free",
  url: "https://easy-poll-time.vercel.app/",
  links: {
    github: "https://easy-poll-time.vercel.app/",
  },
};
